# 4D 视觉联合对准现场复测问题与修复记录

- **日期：** 2026-07-26
- **工程：** `robot_visual` 上位机
- **关联设计：** `2026-07-25-4d-vision-joint-alignment-pick-design.md`
- **关联计划：** `2026-07-25-4d-vision-joint-alignment-pick-implementation-plan.md`
- **现场日志：** `2026-07-26+log (2).txt`
- **状态：** Z 稳定问题已修复并通过自动化测试；Rz 和目标追踪问题按现场决策延期处理

## 1. 记录目的

4D 联合视觉对准逻辑完成后，现场将最大联合精修次数设置为 2 进行了多轮实机测试。测试表明：

1. 部分任务能够在一至两次联合精修内完成抓取；
2. 部分任务出现 Rz 正负往复变化；
3. 部分任务的 XY/Rz 已进入阈值，但 Z 三帧极差始终无法满足原条件；
4. 有一次任务因锁定目标单帧丢失而立即停止。

本记录用于说明三个问题的现场证据、讨论过程、最终决策、实际代码改动和待验证事项，避免只保留代码而缺少问题追溯。

## 2. 现场结果概览

最大联合精修次数为 2 的八轮测试中：

| 分类 | 次数 | 说明 |
|---|---:|---|
| 成功 | 4 | 其中两轮使用了第二次联合精修 |
| Rz 往复变化后次数耗尽 | 2 | 出现 `+24° → -8° → +6°` 一类变化 |
| XY/Rz 已满足但 Z 未稳定 | 1 | 平面残差满足阈值，Z 原始三帧极差未在 8 秒内持续满足 |
| 锁定目标暂时丢失 | 1 | 视觉仅保留旁站候选，上位机拒绝切换并停止 |

本轮没有通过放宽 XY/Rz 阈值、增加精修次数或延长 8 秒窗口来提高表面成功率。

## 3. Rz 往复变化

### 3.1 现场证据

典型过程包括：

```text
+24.25° → 下发 -24.25° → -8.13°
+23.96° → 下发 -23.96° → -7.77°
+17.14° → 下发 -17.14° → -5.40°
+17.97° → 下发 -17.97° → -6.71°
```

Rz 在修正后越过零点，说明当前方向并非简单取反错误，但修正量、旋转中心、视觉角度响应或工具坐标关系仍可能存在系统误差。

### 3.2 补充现场条件

当前尚未完成“夹爪抓取中心到机械臂末端”的 TCP 标定，现有代码主要通过 Z 余量补偿夹爪长度。该做法不能覆盖：

- 夹爪中心相对末端的 X/Y 偏心；
- 实际工具 Z 轴方向误差；
- 绕 Rz 旋转时的真实抓取中心偏移；
- 新 TCP 与 C++ 手眼矩阵工具坐标系不一致。

现场还观察到：料箱相对夹爪较正、所需 Rz 旋转较小时，通常更容易满足 XY/Rz 阈值。该现象与“大角度旋转放大错误 TCP 旋转中心带来的 XY 耦合”一致，但不能据此认定 TCP 是唯一原因。

### 3.3 最终决策

当前不修改 Rz：

- 不加入临时 `0.75` 修正系数；
- 不放宽 Rz 阈值；
- 不增加联合精修次数；
- 不修改 Python 角度算法。

先完成夹爪抓取中心 TCP 标定，并确认新 TCP 与 C++ 手眼矩阵使用同一工具坐标系。标定后重新采集“精修前视觉 Rz、下发工具 Rz、运动前后实际 TCP、运动后稳定视觉 Rz”，再判断是否需要角度比例补偿。

## 4. XY/Rz 已满足但 Z 无法稳定

### 4.1 原逻辑

每个联合 MoveJ 或 MoveL 到位后：

```text
最少观察 4 秒
最多观察 8 秒
只接收 frame_id 和 timestamp 严格递增的真实新帧
最近 3 个真实 Z 样本最大值减最小值不大于 5mm
```

该规则对连续单帧深度异常较敏感。只要最近三帧中混入一个高异常值或低异常值，本次窗口就不能判稳。

### 4.2 失败样本

现场失败轮次在约 4.56 秒时，最近五个真实 Z 样本为：

```text
[947.0, 943.5, 942.1, 934.4, 948.7] mm
```

五帧排序后：

```text
[934.4, 942.1, 943.5, 947.0, 948.7] mm
```

去掉一个最低值和一个最高值后：

```text
[942.1, 943.5, 947.0] mm
```

核心三帧极差为：

```text
947.0 - 942.1 = 4.9mm
```

核心结果已经满足原 5mm 稳定边界，但原“最近连续三帧原始极差”会被两端异常值反复打断，最终在 8 秒处停止。

### 4.3 最终修复

新的 Z 判稳规则为：

```text
保留最近 5 个真实新帧
→ 对 Z 排序
→ 去掉一个最大值和一个最小值
→ 中间 3 帧核心极差不大于 5mm
→ 使用 5 帧中值作为最终下探深度
```

保持不变的安全边界：

- 最少观察时间仍为 4 秒；
- 最大观察时间仍为 8 秒硬上限；
- 核心稳定极差仍为 5mm，没有直接放宽；
- 重复 HTTP 缓存帧仍不计入样本；
- XY/Rz 必须同时满足运行时阈值；
- 目标必须仍然有效；
- Z 下探方向、抓取余量和硬上限公式保持不变。

### 4.4 日志变化

统一窗口日志由：

```text
ZRange=...
```

调整为：

```text
ZCoreRange=...
ZMedian=...
ZSamples=[...]
```

现场可以同时判断原始五帧、核心极差和最终采用的中值。

## 5. 锁定目标暂时丢失

### 5.1 现场证据

故障帧中，上一锁定锚点约为：

```text
(-45.4, 215.5) mm
```

下一帧仅剩一个候选，其锁定距离约为：

```text
372.9mm
```

该候选明显不是上一锁定目标。C++ 拒绝切换到旁站目标并停止调度，避免抓取错误工位目标。

### 5.2 能力边界

当前 C++ 所谓“锁定”是基于工位锚点、层高和距离的候选关联，不是真正的视觉追踪器。目标不再出现在 Python 检测结果中时，上位机无法凭空恢复其位置，也不能证明后续重新出现的候选仍是同一个物体。

曾讨论过“允许临时丢失若干帧并继续原 4～8 秒窗口”，但该策略会带来以下问题：

- 可能混用目标丢失前后的 Z 稳定证据；
- 可能使用历史坐标掩盖视觉算法漏检；
- 没有稳定 `track_id` 时无法证明重新出现的是同一个目标；
- 增加上位机冗余状态，却没有修复视觉端根因。

### 5.3 最终决策

当前版本保持原安全行为：

```text
ValidateVisionAlignment 状态内锁定目标丢失
→ 立即停止视觉等待
→ 通过统一错误出口停止调度
→ 不下发精修
→ 不下探
→ 不切换旁站目标
→ 不自动重试
```

不在本轮增加上位机丢帧等待策略。

### 5.4 远期视觉方案

更换新工控机并提升 Python 帧率后，再评估真正的目标追踪器。建议视觉接口至少提供：

```text
track_id
track_state
lost_frames
frame_id
timestamp
目标位置、深度、角度和置信度
```

机械臂静止的观察窗口内由 Python 维护目标 ID；跨机械臂运动阶段仍需结合固定工位锚点或机器人位姿补偿，不能只依赖图像像素位置关联。

## 6. 实际代码变更

### 6.1 生产代码

- `src/visionalignmentdecision.h`
  - 增加稳健深度判定结果结构和纯判定接口。
- `src/visionalignmentdecision.cpp`
  - 实现五帧排序、非法值拒绝、核心三帧极差和五帧中值计算。
- `src/huayanScheduler.cpp`
  - Z 窗口由三帧改为五帧；
  - 使用稳健深度结果判断 `zStable`；
  - 下探使用五帧中值；
  - 增加 `ZCoreRange` 和 `ZMedian` 日志；
  - 目标丢失保持原立即安全停止。
- `src/huayanScheduler.h`
  - 更新 Z 样本窗口语义注释。

### 6.2 自动化测试

- `tests/test_vision_alignment_decision.cpp`
  - 覆盖窗口不完整；
  - 覆盖一高一低异常值；
  - 覆盖核心分布仍过宽。
- `tests/test_stable_z_validation_contract.cpp`
  - 更新五帧稳健判定、日志和中值下探契约。
- `tests/test_depth_descent_contract.cpp`
  - 确认只有统一窗口返回下探后才能使用稳健中值进入原下探流程。
- `tests/test_huayan_scheduler_contract.cpp`
  - 保持目标可信拒绝立即进入安全失败的契约。

## 7. 提交与验证记录

相关提交：

```text
cce7f87 修复视觉深度判稳和目标临时丢失处理
b88452c 恢复验证窗口目标丢失立即停止
```

`cce7f87` 曾同时包含 Z 修复和临时丢失等待策略；现场讨论后认为目标丢失属于缺少追踪器的算法能力问题，因此通过 `b88452c` 恢复原立即安全停止。两个提交叠加后的最终有效行为是：

```text
保留五帧稳健 Z 修复
目标丢失保持原立即安全停止
```

验证环境：

```text
Qt 6.8.3
MSVC 2022 64 位
Debug 构建
QT_QPA_PLATFORM=offscreen
```

验证结果：

```text
完整工程构建成功
CTest：22/22 通过
未执行 git push
```

## 8. 现场复测要求

下一轮现场测试重点记录：

1. 每个观察窗口的 `ZSamples`、`ZCoreRange` 和 `ZMedian`；
2. XY/Rz 已满足时，是否能在 4 至 8 秒内稳定进入下探；
3. 最终下探使用的 Z 中值与现场实际抓取深度是否匹配；
4. 目标丢失时是否立即停止，且没有精修、下探、换目标或自动重试；
5. 完成夹爪 TCP 标定后，重新统计 Rz 修正前后残差。

## 9. 本轮明确未做

- 未修改 Python 视觉服务；
- 未增加目标追踪器或 `track_id`；
- 未修改 Rz 修正比例；
- 未放宽 XY/Rz 阈值；
- 未增加最大联合精修次数；
- 未延长 8 秒硬上限；
- 未修改 Z 下探方向、抓取余量和硬安全上限；
- 未扩展到 Rx/Ry 或 6D 抓取。
