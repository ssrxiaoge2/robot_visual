# `robot_visual` 智能体协作与工程规则

## 1. 适用范围与规则优先级

- 本文件适用于本仓库内的需求分析、代码修改、测试、文档和 Git 操作。
- 本项目是基于 Qt 6 和 C++17 的工业机器人上位机，支持 Windows 和 Linux，涉及 AGV、机械臂、视觉、扫码、托盘、主调度和充电等功能。
- 用户在当前任务中的明确要求优先于本文件；本文件优先于历史计划、规格和报告。
- 当前源码、`CMakeLists.txt`、实际测试和现场已验证结果是判断项目现状的依据。历史文档与当前实现冲突时，不得照搬历史文档。
- 优先采用小而明确的增量修改，保留用户已有改动，不修改与当前目标无关的代码。
- 只有当缺失信息会改变架构、安全行为、外部接口或验收结果时才暂停询问；其余情况应基于现有项目事实作合理假设并继续。

## 2. 任务分析与实施方式

- 回答、解释、审查和状态报告类任务以只读检查为主，未经要求不修改代码。
- 诊断类任务先定位根因并提供证据；用户只要求分析时，不直接实施修复。
- 修改和新增功能类任务应先确认目标、非目标、现有入口、影响范围和验收标准，再实施最小范围变更。
- 用户已经确认的设计结论不得重复进行开放式讨论。只有发现矛盾、新的安全风险或现有实现无法满足结论时才重新确认。
- 简单修改、注释补充、局部 UI 调整、配置修正和单点缺陷默认不生成规格或实施计划，但必须按照实际影响留下 Git、变更日志或缺陷记录。
- 中大型功能、跨模块功能和安全关键功能需要先明确规格；一般功能的实施计划默认拆分为 1 至 3 个可独立验收的纵向任务。
- 复杂跨模块功能可以根据真实依赖增加任务数量，但不得按类或文件机械拆分，并应优先合并共享上下文较多的连续步骤。
- 规格负责描述业务目标、边界、安全约束和验收标准；计划负责描述交付结果、修改范围、实施顺序和验证方式。两者不得重复堆叠逐函数实现细节。
- 不得为了满足计划形式而新增无业务价值的类、接口、文档、测试层或抽象。

## 3. 任务拆分与子代理规则

- 默认由当前主代理直接执行任务。只有用户明确要求、适用技能明确要求，或存在两个以上真正独立且适合并行的子任务时，才考虑使用子代理。
- 小型修改、注释、局部 UI、单点缺陷，以及必须连续修改同一组文件的任务，不使用子代理。
- 任务按可验收的纵向结果拆分，不按类、文件、函数或测试文件数量拆分。
- 复杂跨模块任务开始前应识别任务依赖。存在前后依赖、共享状态机、共享可变状态或修改相同文件时必须串行执行。
- 只有接口已经稳定、彼此不依赖未完成代码、没有文件冲突且能够独立验证的任务，才考虑并行执行。
- 多个步骤如果共享大量上下文、依赖同一状态机或会修改相同文件，应合并为同一任务并由同一代理完成。
- 同一任务的实现、问题修复和定向复测优先由同一个代理持续完成，不因首次测试失败更换代理。
- “同一时间最多一个子代理”不限制复杂功能串行使用多个子代理；前一任务完成、验证并交接后，可以根据下一任务边界启用新的子代理。
- 连续任务大量复用同一文件、接口和设计上下文时，优先让原代理继续执行，不为阶段形式更换代理。
- 中型功能默认最多使用一个实现代理，并在整体完成后进行一次综合审查。
- 除安全关键功能外，不为每个子任务重复安排规格审查、质量审查和多轮复审。
- 安全关键功能可增加一次独立的安全边界审查，但实现修复仍优先交回原实现代理，避免重新建立上下文。
- 子代理不得自行继续派生子代理，除非用户明确授权。
- 子代理只读取任务指定的规格、文件、直接调用链和相关测试，不重新扫描整个仓库或重复阅读全部历史对话。
- 给子代理的任务包必须包含目标、事实来源、允许修改范围、禁止修改范围、验收标准和测试命令，不依赖隐含上下文。
- 子代理最终只报告修改文件、关键决策、验证结果和遗留风险，不重复复述完整规格和计划。
- 构建和全量测试默认由主代理统一执行。子代理只运行完成其任务所必需的定向测试，除非任务明确要求完整构建。
- 主代理在每个依赖边界检查修改范围、接口契约和定向测试结果；这属于集成检查，不自动扩展成多代理正式审查。
- 新的公共接口会影响多个后续任务，或当前阶段涉及设备安全、线程生命周期、硬件状态机和不可逆动作时，可以在该依赖边界增加一次轻量接口审查或安全审查。
- 适用技能要求使用子代理但没有规定执行方式时，应先询问用户采用串行还是并行。
- 由于不同电脑性能不同，在启用并行子代理前必须先说明并行收益、资源消耗和文件冲突风险，并征求用户同意。
- 未获得本次任务的并行授权时，同一时间最多运行一个子代理。

## 4. 上下文与 Token 控制

- 搜索文件和文本优先使用 `rg` 与 `rg --files`，先缩小范围再读取文件。
- 优先读取本次差异、相关接口、直接调用链和对应测试，不默认读取整个大文件、全部文档或全部历史提交。
- 已有规格、计划、现场报告和测试可作为单一事实来源时，后续任务应引用文件和章节，不重复复制全文。
- 进度更新保持简短，只报告新发现、关键决策、风险、验证结果和阻塞。
- 不重复解释用户已经确认的需求，不为展示过程而输出冗长的文件清单、工具调用过程或通用教程。
- 定向测试在开发阶段按需运行；完整构建和全量测试原则上在最终集成、提交前或用户要求时统一执行。
- 并行执行不能以增加重复上下文、重复构建或同文件冲突为代价。没有真实并行收益时使用串行执行。

## 5. 代码结构与职责边界

- UI 层负责展示、输入收集和事件转发，不直接持有设备协议细节或复制业务状态机。
- 设备通信、寄存器、SDK 调用和连接生命周期保留在相应设备控制类或适配层中，其他模块通过明确接口调用。
- 主调度、任务队列和任务执行逻辑应与单设备调试功能解耦；新增功能关闭时不得改变原有调度行为。
- 同一设备或同一安全流程只能有一个命令所有者。手动入口、自动入口和关闭入口应汇入同一套底层时序，避免并发发送和双状态机。
- 新增类必须解决明确的职责、所有权、测试隔离或耦合问题。简单转发、单个 IO 或少量条件判断不得为了形式单独建类。
- 修改大类时只增加当前功能必要的集成代码。只有现有结构已经直接阻碍本次功能的正确性或测试性时，才进行有边界的局部整理。
- 跨线程、异步回调和 QObject 生命周期必须明确对象所有权、取消路径、重复回调处理和销毁行为。
- 不猜测硬件寄存器、单位、量程、SDK 语义和安全阈值。现场验证代码、设备手册和用户确认值的优先级高于模型推测。

## 6. 安全关键功能

- 机械臂运动、AGV 导航、充电、设备启停、IO、应用关闭和故障恢复均属于安全关键范围。
- 安全关键流程必须明确前置条件、唯一命令所有者、正常终态、失败终态、超时、取消、重入和应用退出行为。
- 异步设备命令应串行化并具备防重入措施；不得由 UI、自动逻辑和关闭逻辑分别直接发送同一设备命令。
- 状态未知、通信失败和确认超时不得自动解释为安全成功。
- 自动重试必须有明确业务依据、次数限制和停止条件；用户要求人工处理时不得擅自增加自动重试。
- 故障处理优先保证停止危险动作、保持必要的调度锁和提供可追溯日志，再考虑恢复业务。
- 不得为了通过测试弱化现场安全检查、删除超时、绕过状态确认或把失败降级为成功。

## 7. C++ 与 Qt 编码规则

- 使用 C++17 和项目当前 Qt 版本，遵循相邻代码的格式和既有 Qt/CMake 模式。
- 默认使用 4 空格缩进；成员变量使用 `m_` 前缀；类名使用 `PascalCase`；函数名使用 `camelCase`。
- 修改现有代码时优先保持当前文件风格，不为统一格式而改动无关行。
- 接口、关键变量、枚举值、结构体字段、协议字段和复杂逻辑需要添加有意义的中文注释。
- 注释应重点说明职责、前置条件、所有权、单位、范围、状态转换、安全边界以及“为什么这样处理”。
- 寄存器地址、位定义、物理单位、缩放比例、超时和阈值必须有来源或语义注释。
- 不为简单赋值、显而易见的 Qt 信号连接、普通 getter/setter 和代码已经完整表达的行为添加重复注释。
- 注释必须与代码一致；修改行为时同步更新相关注释，禁止保留误导性的历史说明。
- 仅补充注释的任务不得顺带修改代码内容、格式或行为，并应通过差异检查证明代码令牌未变化。

## 8. 文档规则

- 新建的规格、计划、报告、检查清单和缺陷记录文件名必须包含当天日期，便于追溯。
- `README.md`、`AGENTS.md`、`CHANGELOG.md` 等固定入口文档不受日期命名规则限制。
- 面向人的说明性文档使用中文；代码标识符、类名、函数名、宏名、测试目标、命令、路径和第三方名称保持原样。
- 引用英文报错、SDK 原文或命令输出时保留原文，并在必要时补充中文解释。
- 文档只记录长期有价值的事实、设计结论、现场证据和验收结果，不重复粘贴大量实现代码或对话过程。
- 修改程序行为的缺陷必须在已有相关报告、规格或变更记录中补充原因、修复方式和验证结果；没有合适文档时新建带日期的中文缺陷记录。
- 纯注释、格式、构建环境操作和不改变程序行为的维护任务默认不新建缺陷文档。
- 纯内部维护以中文 Git 提交及代码差异作为基础追溯；用户可见变化升级到 `CHANGELOG.md` 或 `README.md`；修改程序行为的缺陷升级到缺陷记录；跨模块设计和安全行为升级到规格、计划及验证报告。
- 如果任务暂不提交且没有其他持久化记录，交付时应明确提示用户选择写入现有维护记录或通过提交留档。
- 每次推送前必须检查 `README.md` 和 `changelog/CHANGELOG.md`。存在用户可见功能、操作方式、配置、部署、硬件行为、安全行为或版本变化时，必须更新相关内容。
- 纯内部重构、注释或测试维护确认没有上述影响时可以不修改入口文档，并在交付说明中明确“已检查，无需更新”，不制造无意义变更。

## 9. 构建与测试

- 执行构建或测试前先读取 [`docs/development/Qt构建与测试环境.md`](docs/development/Qt构建与测试环境.md)。该文档记录的是党欢电脑的固定路径，路径存在时直接使用，避免重复探测环境。
- 文档中的固定路径仅适用于党欢电脑；路径不存在时不得强行套用或视为项目错误，应复用当前电脑已有构建缓存和 Qt Kit。
- 修改前先确认当前操作系统、有效构建目录、`CMakeCache.txt`、Qt 套件和编译器，不重复搜索或安装已经明确提供的工具。
- 已存在有效构建目录时直接构建该目录，不对只用于容纳子目录的外层 `build` 目录执行构建。
- CTest 必须在包含 `CTestTestfile.cmake` 的实际构建目录运行。
- 测试失败时先读取完整错误并定位根因，不通过修改测试期望、跳过测试或扩大超时掩盖真实问题。
- 如果测试被操作系统安全策略、硬件缺失或外部依赖阻止，应明确区分“未运行”和“测试失败”，报告证据并按用户决定处理。
- 完成声明必须基于当前改动后的新鲜验证结果。构建成功不能替代测试通过，定向测试通过也不能声称全量测试通过。

## 10. Git 与变更管理

- 开始修改前检查分支和工作区状态。用户已有修改不得覆盖、回退或混入无关提交。
- 智能体新建功能分支时默认使用 `codex/` 前缀；用户或团队已经指定分支、命名规范或工作分支时沿用现有约定，不擅自改名或另建分支。
- 禁止使用 `git reset --hard`、强制检出等破坏性命令清理用户工作区。
- 提交前检查实际差异、暂存文件、构建和测试结果，确保没有构建产物、日志、临时文件或无关修改。
- 提交信息使用中文，说明本次交付结果。小型行为修改通常形成一个提交，提交正文应简要记录原因、修改和验证。
- 大型跨模块功能默认按 2 至 3 个可独立验证的功能里程碑提交，例如底层能力、业务接入和最终集成；不得按文件、类、子代理数量或计划步骤机械提交。
- 每个里程碑提交应具有明确边界，并尽量保持可构建、可定向验证和可独立回退。规格、计划和注释不为了增加提交数量而单独提交，除非用户明确要求保留阶段记录。
- 代码可以提交，但不得私自推送。只有用户明确同意或直接要求 push 时才执行推送。
- 推送前执行本文“文档规则”中的入口文档影响检查，需要更新时先更新并验证。
- 推送后报告远端、分支和提交，不自动创建 Pull Request，除非用户明确要求。

## 11. 项目简短导航

以下路径只用于快速定位，实际模块和文件以当前源码及 `CMakeLists.txt` 为准：

- `CMakeLists.txt`：程序、依赖、平台条件和测试入口。
- `src/mainwindow.*`：主窗口 UI、状态展示和用户事件转发。
- `src/devicemanager.*`：设备生命周期、跨模块接线和顶层业务入口。
- `src/agvcontroller.*`：仙工 AGV 的 Modbus 通信和车辆侧状态/IO。
- `src/huayanScheduler.*`：华沿机械臂 SDK 调度与动作流程。
- `src/linemanager.*`、`src/taskexecutor.*`、`src/taskqueue.*`：主调度、任务状态机和 FIFO 队列。
- `src/visionclient.*` 及视觉相关模块：视觉 HTTP、目标选择、坐标和对齐计算。
- `src/pallet*`：托盘配置、占用状态和取放流程。
- `src/nscanscheduler.*`：扫码设备及扫码流程。
- `src/customSysScheduler.*`：外部客户系统通信与业务数据获取。
- `src/charge*`、`src/autochargecoordinator.*`：充电配置、协议、控制、安全收尾和自动策略。
- `src/settingsmanager.*` 及设置对话框：运行参数持久化与配置界面。
- `tests/`：单元测试、契约测试和安全回归测试。
- `docs/development/Qt构建与测试环境.md`：党欢电脑的Windows和Linux固定构建、CTest环境。
- `docs/superpowers/`：按日期保存的规格、计划、报告和现场验证记录。
- `changelog/CHANGELOG.md`：版本和用户可见变更记录。

## 12. 默认完成标准

- 实现满足当前任务的目标和非目标，没有修改无关逻辑。
- 关键接口、状态、单位、安全逻辑和复杂分支已有准确中文注释。
- 相关定向测试通过；需要完整验证时，构建和全量测试结果已明确记录。
- 已检查差异，不包含生成物、日志、临时文件、意外格式化或用户未授权的修改。
- 文档与实际行为一致，遗留风险、未运行测试和现场待验证项已如实说明。
- 未经用户明确授权不推送、不创建 PR、不合并分支。
